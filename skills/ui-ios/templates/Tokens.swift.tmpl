// Generated by ui-ios skill. Anchors: {{ANCHORS}}
// DO NOT add magic numbers in your views. Add tokens here instead.
// Every padding, frame, cornerRadius, font, and color in the app must reference Theme.*

import SwiftUI

enum Theme {

    // MARK: - Color
    //
    // All colors come from the asset catalog so they support Light + Dark variants.
    // Add a Color Set in Assets.xcassets for each name below before shipping.
    //
    enum Color {
        static let background      = SwiftUI.Color("Background")
        static let surface         = SwiftUI.Color("Surface")
        static let surfaceElevated = SwiftUI.Color("SurfaceElevated")

        static let textPrimary     = SwiftUI.Color("TextPrimary")
        static let textSecondary   = SwiftUI.Color("TextSecondary")
        static let textTertiary    = SwiftUI.Color("TextTertiary")

        static let accent          = SwiftUI.Color("Accent")
        static let accentSubtle    = SwiftUI.Color("AccentSubtle")
        static let onAccent        = SwiftUI.Color("OnAccent")

        static let border          = SwiftUI.Color("Border")
        static let borderSubtle    = SwiftUI.Color("BorderSubtle")

        // Semantic
        static let success         = SwiftUI.Color("Success")
        static let successSubtle   = SwiftUI.Color("SuccessSubtle")
        static let warning         = SwiftUI.Color("Warning")
        static let warningSubtle   = SwiftUI.Color("WarningSubtle")
        static let danger          = SwiftUI.Color("Danger")
        static let dangerSubtle    = SwiftUI.Color("DangerSubtle")
    }

    // MARK: - Spacing
    //
    // 4 / 8 grid only. No odd values. If you need a value that isn't here,
    // ASK before extending — every addition is a design decision.
    //
    enum Spacing {
        static let xxs: CGFloat = 2
        static let xs:  CGFloat = 4
        static let sm:  CGFloat = 8
        static let md:  CGFloat = 12
        static let lg:  CGFloat = 16
        static let xl:  CGFloat = 20
        static let xxl: CGFloat = 24
        static let xxxl: CGFloat = 32
        static let huge: CGFloat = 48
        static let massive: CGFloat = 64
    }

    // MARK: - Radius
    //
    // ≤ 4 values per project. Pick from your anchor recipe.
    //
    enum Radius {
        static let none: CGFloat = 0
        static let sm:   CGFloat = 8
        static let md:   CGFloat = 12
        static let lg:   CGFloat = 16
        static let xl:   CGFloat = 24
    }

    // MARK: - Font
    //
    // Always returns a Dynamic-Type-aware font. Never use .system(size:) directly
    // outside this enum — that breaks Dynamic Type support.
    //
    // For brand custom fonts: declare here with a .system(...) fallback inside
    // the function body via Font.custom(_, size:relativeTo:).
    //
    enum Font {
        // Display — large hero
        static func display() -> SwiftUI.Font {
            .system(.largeTitle, design: .default).weight(.semibold)
        }
        static func displayLarge() -> SwiftUI.Font {
            .system(size: 44, weight: .bold, design: .default)
                .leading(.tight)
        }

        // Title scale — section heads
        static func title() -> SwiftUI.Font   { .system(.title,  design: .default).weight(.semibold) }
        static func title2() -> SwiftUI.Font  { .system(.title2, design: .default).weight(.semibold) }
        static func title3() -> SwiftUI.Font  { .system(.title3, design: .default).weight(.semibold) }

        // Body scale
        static func headline() -> SwiftUI.Font   { .system(.headline,  design: .default) }
        static func body() -> SwiftUI.Font       { .system(.body,      design: .default) }
        static func callout() -> SwiftUI.Font    { .system(.callout,   design: .default) }
        static func subheadline() -> SwiftUI.Font { .system(.subheadline, design: .default) }
        static func footnote() -> SwiftUI.Font   { .system(.footnote,  design: .default) }
        static func caption() -> SwiftUI.Font    { .system(.caption,   design: .default) }

        // Mono — for code surfaces only
        static func mono() -> SwiftUI.Font {
            .system(.body, design: .monospaced)
        }
    }

    // MARK: - Shadow
    //
    // Anchor-controlled. Linear / Editorial / Brutalist set all of these to .none.
    //
    struct ShadowSpec {
        let color: SwiftUI.Color
        let radius: CGFloat
        let x: CGFloat
        let y: CGFloat
    }

    enum Shadow {
        static let none = ShadowSpec(color: .clear, radius: 0, x: 0, y: 0)
        static let sm   = ShadowSpec(color: SwiftUI.Color.black.opacity(0.05), radius: 2,  x: 0, y: 1)
        static let md   = ShadowSpec(color: SwiftUI.Color.black.opacity(0.08), radius: 12, x: 0, y: 4)
        static let lg   = ShadowSpec(color: SwiftUI.Color.black.opacity(0.12), radius: 24, x: 0, y: 8)
    }

    // MARK: - Motion
    //
    // Durations in seconds. Animations expressed as ready-to-pass SwiftUI.Animation values.
    // Reference these from .animation(_, value:) and withAnimation { ... } — never write
    // a literal duration in a View body.
    //
    enum Motion {
        static let instant: Double = 0.0
        static let fast:    Double = 0.06
        static let base:    Double = 0.20
        static let slow:    Double = 0.32
        static let sheet:   Double = 0.40

        static let easeOut    = Animation.easeOut(duration: base)
        static let easeOutFast = Animation.easeOut(duration: fast)
        static let easeInOut  = Animation.easeInOut(duration: base)
        static let smooth     = Animation.smooth(duration: base)

        // Apple-native spring — only on Arc / Things 3 / Apple Music anchors.
        static let spring = Animation.interactiveSpring(
            response: 0.32,
            dampingFraction: 0.72,
            blendDuration: 0
        )

        // Sheet / large layout
        static let springLarge = Animation.interactiveSpring(
            response: 0.42,
            dampingFraction: 0.78,
            blendDuration: 0
        )
    }
}

// MARK: - View extensions for ergonomics

extension View {
    /// Apply a Theme.ShadowSpec uniformly.
    func themeShadow(_ spec: Theme.ShadowSpec) -> some View {
        self.shadow(color: spec.color, radius: spec.radius, x: spec.x, y: spec.y)
    }
}

// MARK: - ThemeModifier (environment-driven)
//
// Inject Theme via the environment so screens never hardcode token paths from
// arbitrary Swift namespaces. See Theme/ThemeModifier.swift for the EnvironmentKey
// pattern. Apply once at the App root:
//
//     WindowGroup {
//         RootView()
//             .withTheme()
//     }
//
