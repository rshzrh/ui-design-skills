# Changelog

## 0.1.0 — 2026-04-08

Initial public release.

- Add `ui-web` skill — React + Tailwind + shadcn/ui slop-free UI generator.
- Add `ui-ios` skill — SwiftUI (iOS 17+) slop-free UI generator.
- Add `ui-android` skill — Jetpack Compose + Material 3 Expressive (API 34+) slop-free UI generator.
- Ship as a Claude Code plugin marketplace (`.claude-plugin/plugin.json` + `marketplace.json`) and as a raw-clone repo with `scripts/install.sh`.
